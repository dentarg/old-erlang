# old-erlang

```bash
brew install vagrant virtualbox # if you don't have them

vagrant up u14 # make note of "Forwarding ports" section

# SSH to machine
vagrant ssh u14

# Install old Erlang and RabbitMQ
/vagrant/install.sh
```
